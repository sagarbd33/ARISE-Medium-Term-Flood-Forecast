# Core scientific stack
numpy>=1.24,<2.0
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3

# Gradient boosting (bias correction + stage conversion)
xgboost>=1.7

# Deep learning (GRU residual correction)
tensorflow>=2.13,<2.16
keras>=2.13

# Meteorological retrieval
ecmwf-api-client>=1.6.3       # TIGGE downloads
cfgrib>=0.9.10                # GRIB decoding (requires eccodes binary)
xarray>=2023.1                # n-D array handling
# eccodes installed via conda: conda install -c conda-forge eccodes

# HEC-DSS interface (Windows-only)
# pydsstools wheels are pre-built for Windows; pip on Linux/macOS may fail
pydsstools>=2.4 ; sys_platform == "win32"

# Operational utilities
schedule>=1.2                 # Daily 08:30 IST trigger
colorama>=0.4.6               # Console colours

# Optional, for notebook-based diagnostics
# jupyter>=1.0
# seaborn>=0.12
